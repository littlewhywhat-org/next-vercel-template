# User flows

`.feature` files. `Background` / `Given` = state. `When` = one action. `Then` = what the user sees.

Validate: `npm run test:e2e:dry`. Cucumber: `npm run test:e2e`.

| | Flow |
|---|---|
| Todos | [todos.feature](flows/todos.feature) |
